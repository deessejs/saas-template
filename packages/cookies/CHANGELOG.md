# @workspace/cookies

## 0.0.1

### Patch Changes

- 8666d16: test(e2e): exercise the new `changesets/action@v2` release pipeline with a trivial patch bump on `@workspace/cookies`. No behavioral change — adds a JSDoc comment to `ConsentCategory` in `packages/cookies/src/types.ts`.
