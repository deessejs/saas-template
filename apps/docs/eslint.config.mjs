import { nextConfig } from "@workspace/eslint-config/next"

export default [
  ...nextConfig,
  {
    ignores: [
      // Generated by fumadocs-mdx at build time. Gitignored
      // (apps/docs/.gitignore:5). Auto-generated TS uses `@ts-nocheck` and
      // `{}` types which violate our strict rules; the source files are owned
      // by fumadocs, not us.
      ".source/**",
    ],
  },
]
